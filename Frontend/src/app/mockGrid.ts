export const data =
   [];
   for(let row=0 ; row<30; row++){
     const currentRow=[];
     for(let col=0;col<50;col++){
       currentRow.push(['a']);
       continue;
     }
     data.push(currentRow);
     }
